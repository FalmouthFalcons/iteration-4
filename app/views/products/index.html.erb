 </br> 
<%= form_tag products_path, :method => 'get' do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
  </p>
<% end %> 
<%# <%= link_to 'ProductToSell', new_product_url %>
<br>
<%# <%= link_to 'Home', welcome_index_path %>

<p class="move_right"><%= link_to 'Back', url_for(:back) %></p>

<h1>My Products</h1>

<p class="move_right">All of your products for sale:</p>
<br>

<button><%= link_to 'List a Product For Sale', new_product_path %> </button>

 <table>
<tr>
    <th>Title </th>
    <th>Category </th>
    <th>Price </th>
    <th>Type </th>
    <th>Delivery </th>
    <th>Quantity Available </th>
    <th> </th>

</tr>
<%# loop for each product on index page  %>
<% @product.each do |product| %>
   <tr>
    <td><%= product.title %> </td>
    <td><%= product.category %> </td>
    <td><%= product.price %> </td>
    <td><%= product.type %> </td>
    <td><%= product.delivery %> </td>
    <td><%= product.active %> </td>
    <td><%= product.quantity %> </td>
    <td><%= link_to 'Show', product_path(product)%> </td>
    <td><%= link_to 'Destroy', product,
            method: :delete,
            data: { confirm: 'Are you sure?' } %></td>
    </tr>

<% end %>
</table>
