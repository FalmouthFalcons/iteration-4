<h1>Shopping Cart</h1>
<br>

<table>
<tr>
    <th>Title </th>
    <th>Category </th>
    <th>Price </th>
    <th>Type </th>
    <th>Delivery </th>
    <th>City </th>
    <th> </th> 

</tr>

<% @final.each do |product| %>

<div class="row">
  <div class="col-3">
   <tr>
        <td><%= product.title %> </td>  
        <td><%= product.delivery %> </td>
        <td><%= product.price %> </td>  
        <td><%= product.city %> </td> 
        <td><%= product.type %> </td> 
        <td><%= product.delivery %> </td> 

   </tr>
   
 </div>

   <%# <%= product.select(price).sum %> 
    

<% end %>
  <% if @product.product_image.present? %>
    <div class="col-9"><%= image_tag @product.product_image %> 
    <% end %>
 

</table>

<div class="row">
  <div class="col-3">
            <p class="move_right"><%= @product.title %> </p>
            <p class="move_right"><%= @product.category %> </p>
            <p class="move_right"><%= @product.description %> </p>

    <p>
        <%= form.submit %>
    </p>


    </div>
 <%# shows image only if one exits %>
 <% if @product.product_image.present? %>
    <div class="col-9"><%= image_tag @product.product_image %> 
 <% end %>